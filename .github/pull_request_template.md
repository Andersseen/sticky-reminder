## What changed

<!-- One or two sentences. What does this do that main does not? -->

## Why

<!-- The problem behind the change. Link an issue if there is one. -->

## Checks

- [ ] `pnpm lint` passes
- [ ] `pnpm test` passes
- [ ] `pnpm test:e2e` passes (and `cd apps/extension && pnpm test:e2e` if the extension changed)
- [ ] Screenshots attached for anything the user can see
